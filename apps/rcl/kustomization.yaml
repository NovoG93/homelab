apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kustomization

namespace: rcl


resources:
- config.yaml
- virtualserver-frontend.yaml
- virtualserver-backend.yaml
- virtualserver-api.yaml
- httproute.yaml
- backend.yaml
- email-outbox-cronjob.yaml
- resource-migration-job.yaml
- frontend.yaml
- db.yaml
- registry-secret.yaml
- mtls-cert.yaml
- mtls-nginx-ca-secret.yaml
- mtls-policy.yaml


generatorOptions:
  disableNameSuffixHash: true

images:
- name: ghcr.io/robotics-content-lab/website/backend
  digest: sha256:d166c5db496ae04f0d90b31dba35798a16de2e30b5189962447f268f67f0d004
- name: ghcr.io/robotics-content-lab/website/frontend
  digest: sha256:bbd1c83f6996de04809a7970c56be7ee6af5cbc124f4314b315c842f0f2730cd

patches:
- target:
    kind: Deployment
  patch: |-
    - op: add
      path: /spec/template/spec/tolerations
      value: []
    - op: add
      path: /spec/template/spec/tolerations/-
      value:
        key: apps
        operator: Equal
        value: "true"
        effect: NoSchedule
- target:
    kind: Deployment
  patch: |-
    - op: add
      path: /spec/template/spec/affinity
      value:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: apps
                    operator: Exists
- target:
    kind: Deployment
  patch: |-
    - op: add
      path: /spec/template/spec/imagePullSecrets
      value:
        - name: ghcr-registry
